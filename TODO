
todo:

    refactor to forms if possible
    task: pass values used in function as parameter and not use it as global state variable
    read into html5 tags that are fitting so i dont recreate div hell
    refactor the meals zoom
    accomodate zoom
    date is stored in utc but db doesnt store in utc when creating something, local db time - convert datetime entries so its uniform
    design the unauthenticated welcome page
    should a try be used for evert request in frontend and on backend?
    implement error handling?
    add loading screen ui
    clean api endpoints to reflect well defined datatypes.
    refactor the usage of dateobject as a get parameter in the food dashbaord page
    change food dashboard selected food to have a fixed datatype so its easier to understand what is passed along
    check if some of the prisma db requests should be transactions
    refactor Workout Creater dropdown to not be global but local
    check if its good practice to handle onclick with {() => function()}
    make date information independent of tab - inter tab communication for date
    add esc and enter button use on ui
    make logged food items grid fixed height and internal scroller
    create force of first setting goal before being able to use the rest of the foodtracking app
    change date for goal creation to global date on client side not date.now
    session db cleanup worker to remove outdated sessions that werent deleted by logout
    add default success/error handling ui for api request
    differenciate between border of button thats clickable and border of a textfield
    improve border on small stuff, make it default border-2, bg neutral 400 and same hover change
    add Ballaststoffe/fiber
    improve ui/ux in trackfood - button enter, show foods i tracked and not just the ones that exist
    fix search in trackfood
    improve ui for the foodlist
    start creating a page for goals that shows how much calories etc
    create a weight page that shows trends
    //make complex food datatype - a meal/recipes - also add information to it for creating it like order of food or so on
    create delete for entries
    add fotos for food and barcodes and extra information
    make ui "boxes" look similar and the same
    make the date not just for one window session but every window session
    find correct background color
    choose button click and hover color
    border color and width
    make text thick
    check hover and transitions
    refactor navbar component into one and just insert middle things
    for bodyweight history list add sorting orders reverse or not reverse etc

